import LuckyDrawClient from "./_client";

export const metadata = { title: "Lucky Draw — Admin" };
export const dynamic = "force-dynamic";

export default function LuckyDrawPage() {
  return <LuckyDrawClient />;
}
